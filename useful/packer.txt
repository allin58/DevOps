в packer можно ставить 

 "provisioners": [
    {
      "type": "shell",
      "inline": [
        "sleep 30",
        "sudo apt-get update",
        "sudo apt install openjdk-8-jdk",
        "sudo apt install nginx",
        "sudo apt install apach2",
        "sudo apt install mysql-server",
        "sudo apt install mysql-client",
        "sudo apt install tomcat7"
      ]
    }
  ]
...................................................